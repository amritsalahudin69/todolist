const bcrypt = require('../../lib/bycrypt.lib')

class UserServices{
    constructor(userRepo) {
        this.userRepo = userRepo
    }

    async findByString(string){
        const allusers
    }
}

module.exports=UserServices